name: Build LineageOS Kernel (WildKSU + SUSFS)

on:
  workflow_dispatch:
    inputs:
      LINEAGE_BRANCH:
        description: "LineageOS branch (e.g. lineage-23.2)"
        required: true
        default: "lineage-23.2"

      ENABLE_NETFILTER:
        description: "Enable NETFILTER (TTL/HL/TPROXY)"
        required: true
        type: boolean
        default: true

      ENABLE_BBR:
        description: "Enable BBR + ECN + FQ"
        required: true
        type: boolean
        default: true

      ENABLE_LSM:
        description: "Enable LSM / BBG"
        required: true
        type: boolean
        default: true

env:
  TZ: Asia/Shanghai
  CLANG_VERSION: clang-r547379
  GCC_TAG: android-12.1.0_r27
  WILDKSU_REF: stable

jobs:
  build:
    name: Build Kernel (WildKSU + SUSFS)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set swap to 10G
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 10

      - name: Setup build environment
        run: |
          set -euxo pipefail

          echo "BUILD_TIME=$(TZ=Asia/Shanghai date "+%y%m%d")" >> "$GITHUB_ENV"
          echo "BUILD_TIME_1=$(TZ=Asia/Shanghai date "+%Y-%m-%d")" >> "$GITHUB_ENV"

          sudo apt-get update
          sudo apt-get install -y \
            git ccache automake flex lzop bison gperf build-essential zip curl \
            zlib1g-dev g++-multilib bzip2 libbz2-dev liblz4-tool make squashfs-tools \
            dpkg-dev libssl-dev python3 bc libc6-dev-i386 libncurses5-dev wget unzip

          mkdir -p "$GITHUB_WORKSPACE/kernel_workspace"

      # ----------------------------
      # Toolchains
      # ----------------------------
      - name: Cache Clang toolchain
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/kernel_workspace/clang-aosp
          key: ${{ env.CLANG_VERSION }}

      - name: Download & extract Clang-aosp (if cache miss)
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace"
          mkdir -p clang-aosp

          wget -O "${CLANG_VERSION}.tar.gz" \
            "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/${CLANG_VERSION}.tar.gz"
          tar -C clang-aosp -zxvf "${CLANG_VERSION}.tar.gz"

      - name: Cache GCC toolchain
        id: cache-gcc
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/kernel_workspace/gcc-64
          key: gcc-aarch64-${{ env.GCC_TAG }}

      - name: Download & extract Gcc-aosp (if cache miss)
        if: steps.cache-gcc.outputs.cache-hit != 'true'
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace"
          mkdir -p gcc-64

          wget -O gcc-aarch64.tar.gz \
            "https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/+archive/refs/tags/${GCC_TAG}.tar.gz"
          tar -C gcc-64 -zxvf gcc-aarch64.tar.gz

      # ----------------------------
      # Kernel source
      # ----------------------------
      - name: Download kernel source (LineageOS oneplus sm8250)
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace"

          git clone --recursive \
            https://github.com/LineageOS/android_kernel_oneplus_sm8250 \
            -b "${{ inputs.LINEAGE_BRANCH }}" \
            android-kernel --depth=1

      # ----------------------------
      # WildKSU
      # ----------------------------
      - name: Set up WildKSU
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/android-kernel"

          rm -rf Wild_KSU
          git clone --depth=1 -b "${{ env.WILDKSU_REF }}" https://github.com/WildKernels/Wild_KSU Wild_KSU

          bash Wild_KSU/kernel/setup.sh "${{ env.WILDKSU_REF }}"

          # keep your original optional patch
          bash "$GITHUB_WORKSPACE/patches/kernel-4.19_5.4-patch.sh" || true

          CONFIG_FILE="arch/arm64/configs/vendor/kona-perf_defconfig"

          set_kcfg() {
            key="$1"
            val="$2"
            file="$3"
            if grep -qE "^${key}=" "$file"; then
              sed -i "s/^${key}=.*/${key}=${val}/" "$file"
            else
              echo "${key}=${val}" >> "$file"
            fi
          }

          set_kcfg "CONFIG_KSU_MANUAL_HOOK" "y" "$CONFIG_FILE"

          KSUVER=$(cd Wild_KSU && expr $(git rev-list --count HEAD) + 30000)
          echo "KSUVER=$KSUVER" >> "$GITHUB_ENV"

          sed -i 's/ -dirty//g' scripts/setlocalversion

      # ----------------------------
      # Fix WildKSU allowlist for kernel 4.19 (TWA_RESUME / put_task_struct)
      # ----------------------------
      - name: Patch WildKSU allowlist for kernel 4.19
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/android-kernel"

          F="drivers/kernelsu/allowlist.c"
          test -f "$F"

          # (A) 清理之前错误插入的那一行（包含 \n 的整行垃圾）
          #     以及任何可能重复插入的 TWA_RESUME 块（避免多次插入）
          sed -i '/TWA_RESUME\\n#define TWA_RESUME 0\\n#endif\\n/d' "$F"

          # (B) 确保 put_task_struct() 声明可见
          if ! grep -qE '^\s*#include\s*<linux/sched/task\.h>' "$F"; then
            if grep -qE '^\s*#include\s*<linux/sched\.h>' "$F"; then
              sed -i '/^\s*#include\s*<linux\/sched\.h>/a #include <linux/sched/task.h>' "$F"
            else
              sed -i '1i #include <linux/sched/task.h>' "$F"
            fi
          fi

          # (C) 老内核没有 TWA_RESUME：在 include 区之后插入真正换行的宏定义
          if ! grep -qE '^\s*#ifndef\s+TWA_RESUME\s*$' "$F"; then
            last_inc_line="$(grep -nE '^\s*#include' "$F" | tail -n 1 | cut -d: -f1 || true)"
            if [ -z "$last_inc_line" ]; then
              last_inc_line=1
            fi

            tmp="$(mktemp)"
            awk -v L="$last_inc_line" '
              NR==L {
                print $0
                print ""
                print "#ifndef TWA_RESUME"
                print "#define TWA_RESUME 0"
                print "#endif"
                print ""
                next
              }
              { print $0 }
            ' "$F" > "$tmp"
            mv "$tmp" "$F"
          fi

          echo "Patched allowlist.c markers:"
          grep -nE 'sched/task\.h|^#ifndef TWA_RESUME|^#define TWA_RESUME' "$F" || true

      # ----------------------------
      # SUSFS (from your zip) - minimal integration (no big patch apply)
      # ----------------------------
      - name: Integrate SUSFS from zip (minimal)
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/android-kernel"

          SUSFS_ZIP="$GITHUB_WORKSPACE/patches/susfs4ksu-kernel-4.19.zip"

          rm -rf /tmp/susfs4ksu
          mkdir -p /tmp/susfs4ksu
          unzip -q "$SUSFS_ZIP" -d /tmp/susfs4ksu

          SUSFS_DIR="$(find /tmp/susfs4ksu -maxdepth 2 -type d -name 'susfs4ksu-kernel-4.19' | head -n 1 || true)"
          if [ -z "$SUSFS_DIR" ]; then
            SUSFS_DIR="$(find /tmp/susfs4ksu -maxdepth 2 -type d -name '*susfs*4.19*' | head -n 1 || true)"
          fi
          test -n "$SUSFS_DIR"

          install -m 0644 "$SUSFS_DIR/kernel_patches/fs/susfs.c" fs/susfs.c
          install -m 0644 "$SUSFS_DIR/kernel_patches/include/linux/susfs.h" include/linux/susfs.h
          install -m 0644 "$SUSFS_DIR/kernel_patches/include/linux/susfs_def.h" include/linux/susfs_def.h

          if ! grep -qE 'obj-\$\(CONFIG_KSU_SUSFS\)[[:space:]]*\+=[[:space:]]*susfs\.o' fs/Makefile; then
            echo "" >> fs/Makefile
            echo "# KernelSU SUSFS" >> fs/Makefile
            echo "obj-\$(CONFIG_KSU_SUSFS) += susfs.o" >> fs/Makefile
          fi

          test -f fs/susfs.c
          test -f include/linux/susfs.h
          test -f include/linux/susfs_def.h
          grep -qE 'obj-\$\(CONFIG_KSU_SUSFS\)[[:space:]]*\+=[[:space:]]*susfs\.o' fs/Makefile

          echo "SUSFS minimal integration done."

      # ----------------------------
      # Optional extensions
      # ----------------------------
      - name: Inject Kernel Extensions
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/android-kernel"
          CONFIG_FILE="arch/arm64/configs/vendor/kona-perf_defconfig"

          set_kcfg() {
            key="$1"
            val="$2"
            file="$3"
            if grep -qE "^${key}=" "$file"; then
              sed -i "s/^${key}=.*/${key}=${val}/" "$file"
            else
              echo "${key}=${val}" >> "$file"
            fi
          }

          if [ "${{ inputs.ENABLE_LSM }}" = "true" ]; then
            curl -fLsS https://raw.githubusercontent.com/vc-teahouse/Baseband-guard/main/setup.sh | bash
            set_kcfg "CONFIG_BBG" "y" "$CONFIG_FILE"
            sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' security/Kconfig
          fi

          if [ "${{ inputs.ENABLE_NETFILTER }}" = "true" ]; then
            set_kcfg "CONFIG_NETFILTER_XT_TARGET_TTL" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_NETFILTER_XT_TARGET_HL" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_NETFILTER_XT_MATCH_HL" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_NETFILTER_XT_MATCH_TPROXY" "y" "$CONFIG_FILE"
          fi

          if [ "${{ inputs.ENABLE_BBR }}" = "true" ]; then
            set_kcfg "CONFIG_TCP_CONG_BBR" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_DEFAULT_BBR" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_IP_ECN" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_TCP_ECN" "y" "$CONFIG_FILE"

            set_kcfg "CONFIG_NET_SCH_DEFAULT" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_NET_SCH_FQ" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_DEFAULT_FQ" "y" "$CONFIG_FILE"
            set_kcfg "CONFIG_DEFAULT_NET_SCH" "\"fq\"" "$CONFIG_FILE"
          fi

      # ----------------------------
      # Build
      # ----------------------------
      - name: Build kernel
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/android-kernel"

          export ARCH=arm64
          export SUBARCH=arm64
          export BRAND_SHOW_FLAG=oneplus
          export PATH="$GITHUB_WORKSPACE/kernel_workspace/clang-aosp/bin:$GITHUB_WORKSPACE/kernel_workspace/gcc-64/bin:$PATH"
          export KBUILD_BUILD_HOST=Github-Action
          export KBUILD_BUILD_USER="$(echo "${{ github.actor }}" | tr A-Z a-z)"

          BA_CMD="CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-android-"
          EX_CMD="LD=ld.lld LLVM=1 LLVM_IAS=1"
          DEFCONFIG="vendor/kona-perf_defconfig vendor/debugfs.config vendor/oplus.config"

          make O=out CC=clang $BA_CMD $EX_CMD $DEFCONFIG
          make -j"$(nproc --all)" O=out CC=clang $BA_CMD $EX_CMD

      - name: Check kernel output
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/android-kernel/out"

          if [ -f arch/arm64/boot/Image ]; then
            echo "KERNEL_VERSION=$(cat include/config/kernel.release)" >> "$GITHUB_ENV"
          else
            echo "Kernel output file is empty"
            exit 1
          fi

      # ----------------------------
      # Package (AnyKernel3)
      # ----------------------------
      - name: Prepare AnyKernel3 and package
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace"

          git clone --depth=1 https://github.com/osm0sis/AnyKernel3

          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
          sed -i 's!BLOCK=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;!BLOCK=auto;!g' AnyKernel3/anykernel.sh
          sed -i 's/IS_SLOT_DEVICE=0;/IS_SLOT_DEVICE=auto;/g' AnyKernel3/anykernel.sh

          rm -rf AnyKernel3/.git* AnyKernel3/README.md

          cp android-kernel/out/arch/arm64/boot/Image AnyKernel3/
          cd AnyKernel3

          zip -r "AK3-${{ inputs.LINEAGE_BRANCH }}-OPlus-SM8250_WildKSU_${{ env.KSUVER }}.zip" ./*
          echo "ZIP_NAME=$(ls *.zip)" >> "$GITHUB_ENV"

      - name: Upload AnyKernel3 ZIP (GitHub Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: AK3-WildKSU
          path: kernel_workspace/AnyKernel3/*.zip

      # ----------------------------
      # Telegram notify
      # ----------------------------
      - name: Send Success to Telegram
        if: success()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE/kernel_workspace/AnyKernel3"

          CAPTION=$'✅ *Kernel Build Success!* \n\n'
          CAPTION+=$'🔸 *Branch:* '"${{ inputs.LINEAGE_BRANCH }}"$'\n'
          CAPTION+=$'🔸 *Kernel Version:* '"${{ env.KERNEL_VERSION }}"$'\n'
          CAPTION+=$'🔸 *KSU Version:* '"${{ env.KSUVER }}"$'\n'

          if [ "${{ inputs.ENABLE_LSM }}" = "true" ]; then CAPTION+=$'🔹 *LSM/BBG:* ✅\n'; else CAPTION+=$'🔹 *LSM/BBG:* ❌\n'; fi
          if [ "${{ inputs.ENABLE_NETFILTER }}" = "true" ]; then CAPTION+=$'🔹 *Netfilter:* ✅\n'; else CAPTION+=$'🔹 *Netfilter:* ❌\n'; fi
          if [ "${{ inputs.ENABLE_BBR }}" = "true" ]; then CAPTION+=$'🔹 *BBR+ECN+FQ:* ✅\n'; else CAPTION+=$'🔹 *BBR+ECN+FQ:* ❌\n'; fi

          curl -fLsS -F chat_id="$TELEGRAM_CHAT_ID" \
            -F document=@"${{ env.ZIP_NAME }}" \
            -F parse_mode=Markdown \
            -F caption="$CAPTION" \
            "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendDocument"

      - name: Send Failure to Telegram
        if: failure()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          set -euxo pipefail

          MESSAGE=$'❌ *Kernel Build Failed!* \n\n'
          MESSAGE+=$'🔸 *Branch:* '"${{ inputs.LINEAGE_BRANCH }}"$'\n'
          MESSAGE+=$'🔸 *Build User:* '"${{ github.actor }}"$'\n'
          MESSAGE+=$'📋 *Logs:* [View Action Run]('"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"$')'

          curl -fLsS -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d parse_mode=Markdown \
            -d text="$MESSAGE"
